# Ota-Cu (v1.0 MVP) 전체 기능 명세서 & 버전별 변경 이력

## 📌 1. 개요 및 실행 정보
- **앱 명칭**: Ota-Cu (서브컬처 일러스트 페스티벌 현장 레이드 & 굿즈 스왑 플랫폼)
- **프로젝트 위치**: `C:\Users\nikor\.gemini\antigravity\scratch\ota-cu`
- **로컬 실행 주소**: `http://localhost:3000`
- **원격 저장소**: `https://github.com/chill7549/ota-cu.git`

---

## 🌟 2. 최신 현재 기능 종합 명세 (Current Full Features)

### 🎨 테마 & UI/UX 디자인 시스템
- **🕊️ 뱁새 테마 (Warm Light) ↔ 🌙 다크 테마 (Dark Mode) 동적 전환**
  - 헤더 우측 상단 토글 버튼으로 실시간 전환.
  - **뱁새 테마**: 포근한 웜 화이트 (`#fbf9f5`) 배경, 차콜 그레이/소프트 브라운 텍스트, 파스텔 핑크 포인트 컬러셋 적용.
  - **다크 테마**: 가독성 높은 다크 슬레이트 톤과 인디고/바이올렛 그래디언트 포인트.
- **📱 디바이스별 완벽 반응형 레이아웃 (반응형 깨짐 완벽 방지)**
  - `min-w-0 flex-1 break-words overflow-hidden` 적용으로 카드 내부 텍스트 튀어나옴, 세로 텍스트 잘림 현상 전면 수정.
  - 핸드폰(1컬럼), 태블릿(2컬럼 그리드), 데스크톱(3컬럼 대시보드 사이드바 뷰) 화면 크기 맞춤 지원.
- **📱 고정 하단 네비게이션 바 (Bottom Navigation Bar)**
  - 3개 핵심 탭 (`🗓️ 스케줄러`, `🚨 현장 레이드`, `🔄 굿즈 스왑`)을 화면 최하단에 고정.
- **📍 플로팅 액션 버튼 (FAB)**
  - 하단 네비게이션 바로 위 (`bottom-20 right-4`)에 둥근 플로팅 형태로 위치 (`+` 제보하기 / `🔄 굿즈 교환 글 올리기`).
- **📐 하단 스크롤 안전 여백 (`pb-40`)**
  - 스크롤 메인 영역 하단에 `pb-40` 패딩을 보장하여 탭 바와 FAB가 마지막 게시글을 가리지 않음.

### 🗓️ [탭 1] 스케줄러 (Calendar Scheduler)
- **2026년 9월 직관적 달력 UI**:
  - 일~토 7일 그리드 방식.
  - **9월 4일~6일** 행사 일정(일러스트코리아 in aT)을 감각적인 날짜 블록 및 메인 카드로 강조 렌더링.
- **원클릭 탭 자동 전환**:
  - 9/4~6일 날짜 블록이나 `🎨 일러스트코리아 in aT` 메인 카드를 클릭하면 즉시 **'🚨 현장 레이드'** 탭으로 자동 화면 전환.

### 🚨 [탭 2] 현장 레이드 (Real-Time Live Raid)
- **실시간 dynamic 경과 시간 (Real-Time Timestamp)**:
  - 게시물 생성 시각(`createdAt`) 기반으로 `방금 전`, `🔥 2분 전`, `15분 전`, `1시간 전` 등의 상대 시간이 실시간(매 10초 타이머)으로 자동 계산되어 화면에 반영됩니다.
- **피드백 & 상태 영구 동적 저장 (LocalStorage Persistence)**:
  - `👍 도움돼요`, `👎 정보가 달라요` 클릭 시 피드백 수치가 고정되지 않고 `localStorage`에 즉시 반영되며, 새로고침 후에도 유지됩니다.
- **내가 작성한 글 식별 & 👑 뱁새 관리자 뱃지**:
  - 현재 유저가 등록한 제보 글에 **`👤 내가 작성함`** 핑크 뱃지와 **`👑 뱁새 관리자`** 황금 뱃지가 부여되며, 핑크빛 강조 테두리 및 `🗑️ 삭제` 버튼이 제공됩니다.
- **저신뢰도 카드 50% 투명화 & 뱃지**:
  - `dislikes >= likes` 시 카드 전체 및 텍스트/이모지 투명도가 **50% (`opacity-50`)로 감소**하며 **`⚠️ 교차검증 필요`** 뱃지 자동 부착.
- **제보하기 태그 박스(Chip) UX & `+` 커스텀 추가**:
  - 클릭 가능한 태그 박스 + 맨 우측 **`+` 태그 박스**로 신규 제보 태그 생성. 가장 최근에 사용한 태그가 **맨 왼쪽(1st)**에, 나머지는 **사용량 순서** 정렬.

### 🔄 [탭 3] 굿즈 스왑 (Goods Swap Exchange)
- **실시간 굿즈 교환 피드 & 내 스왑 글 표시**:
  - 내가 올린 교환 글에 **`👤 내가 작성함`** 뱃지와 강조 테두리가 부여됩니다.
  - 실시간 경과 시간 계산 및 `localStorage` 영구 반영.
- **수익 모델 (100원 확성기) 상단 고정 정렬**:
  - `isBoosted: true` ('📢 100원 확성기로 상단 노출 중') 속성 카드가 피드 **무조건 최상단(1~2위)**에 정렬.
- **카테고리 태그 박스(Chip) UX & `+` 커스텀 추가**:
  - 카테고리 태그 박스 + **`+` 커스텀 태그** 생성. 최근 사용 카테고리 **맨 왼쪽(1st)**, 나머지는 **사용 빈도순** 정렬.

### ℹ️ aT센터 인프라 & 부스 배치도 모달
- **헤더 우측 [ℹ️ 인프라] 버튼**:
  - 부스 배치도(A~E구역), 코인 락커(지하 1층), ATM 위치(1층 로비) 안내.

---

## 📜 3. 버전별 히스토리 & 변경 이력 (Version History)

### 🔹 v1.0 MVP (초기 깃허브 개발자 연동 버전을 기반으로 시작)
- 개발자 컨퍼런스/깃허브 연동 기반의 2개 탭 (실시간 레이드 & 굿즈 스왑) 구현.

### 🔹 v1.1 (서브컬처 일러스트 페스티벌 컨셉 전면 전환)
- 개발자 용어 전면 제거 및 '일러스트코리아 in aT' 서브컬처 페스티벌 컨셉 전환. 3대 지표 및 노란 펄스 인디케이터 적용.

### 🔹 v1.2 (3개 탭 확장, 캘린더 UI, 인프라 모달 & 확성기 정렬)
- 스케줄러 탭 신설, aT센터 인프라 모달 추가, 100원 확성기 게시글 상단 고정 정렬 구현.

### 🔹 v1.3 (뱁새 테마 토글, Bottom Nav & 신뢰도 시스템)
- `🕊️ 뱁새` 웜화이트 테마 토글, 최하단 고정 Bottom Nav, FAB 위치 조정, 정보 신뢰도 시스템(`👍`/`👎`) 도입.

### 🔹 v1.4 (저신뢰도 50% 투명화, ⚠️ 뱃지 & 동적 태그 박스 UX)
- 저신뢰도 카드 50% 투명화 및 `⚠️ 교차검증 필요` 뱃지, 제보/스왑 태그 박스 UX (최근 사용 맨 왼쪽 & 사용량순 나열), 폴더명 `ota-cu` 변경.

### 🔹 v1.5 (반응형 레이아웃 텍스트 튀어나옴 수정, 실시간 dynamic 시간 & 내 작성글/관리자 뱃지)
- **반응형 layout 깨짐 완벽 수정**: 카드가 늘어날 때 텍스트나 버튼이 튀어나오거나 세로로 꺾이는 현상을 `min-w-0 flex-wrap break-words`로 전면 개편.
- **실시간 경과 시간 & 피드백 동적 연동**: 게시글 시각(`createdAt`) 기반 실시간 relative timestamp 반영 및 `👍`/`👎` 수치 `localStorage` 영구 반영.
- **내 작성글 식별 & 👑 뱁새 관리자 뱃지**: 작성한 게시글에 **`👤 내가 작성함`** 핑크 뱃지와 **`👑 뱁새 관리자`** 황금 뱃지, 분홍 강조 테두리, `🗑️ 삭제` 버튼 제공.

# Ota-Cu (v1.0 MVP) 전체 기능 명세서 & 버전별 변경 이력

## 📌 1. 개요 및 실행 정보
- **앱 명칭**: Ota-Cu (서브컬처 일러스트 페스티벌 현장 레이드 & 굿즈 스왑 플랫폼)
- **프로젝트 위치**: `C:\Users\nikor\.gemini\antigravity\scratch\ota-cu`
- **로컬 실행 주소**: `http://localhost:3000`
- **원격 저장소**: `https://github.com/chill7549/ota-cu.git`
- **라이브 배포 주소**: `https://chill7549.github.io/ota-cu/`

---

## 🌟 2. 최신 현재 기능 종합 명세 (Current Full Features)

### 🎨 테마 & UI/UX 디자인 시스템
- **📱 모바일 상단 다이나믹 아일랜드 / 노치 / 상태바 안전 여백 (Safe Area Inset)**
  - `env(safe-area-inset-top)` & `env(safe-area-inset-bottom)` 시스템 변수 완벽 연결.
  - 아이폰 다이나믹 아일랜드, 상단 노치/상태바(시계, 와이파이, 배터리 아이콘), 하단 스와이프 홈 바(Home Indicator)와 상단 헤더 및 하단 네비게이션 바가 겹치지 않도록 안전 여백(`header-safe-top`, `nav-safe-bottom`, `fab-safe-bottom`) 자동 부여.
- **🕊️ 뱁새 테마 (Warm Light) ↔ 🌙 다크 테마 (Dark Mode) 동적 전환**
  - 헤더 우측 상단 토글 버튼으로 실시간 전환.
  - **뱁새 테마**: 포근한 웜 화이트 (`#fbf9f5`) 배경, 차콜 그레이/소프트 브라운 텍스트, 파스텔 핑크 포인트 컬러셋 적용.
  - **다크 테마**: 가독성 높은 다크 슬레이트 톤과 인디고/바이올렛 그래디언트 포인트.
- **📱 디바이스별 완벽 반응형 레이아웃**
  - `min-w-0 flex-1 break-words overflow-hidden` 적용으로 카드 내부 텍스트 튀어나옴, 세로 텍스트 잘림 현상 전면 수정.
  - 핸드폰(1컬럼), 태블릿(2컬럼 그리드), 데스크톱(3컬럼 대시보드 사이드바 뷰) 화면 크기 맞춤 지원.
- **📱 동적 스크롤 하단 네비게이션 바 & FAB (Scroll Hide/Show Navigation)**
  - **기본 상태**: 화면 하단에 안전 여백(`nav-safe-bottom`, `fab-safe-bottom`)과 함께 고정되어 접근성 유지.
  - **스크롤 다운 (Scroll Down)**: 타임라인 및 피드 감상 시 시야를 방해하지 않도록 하단 네비게이션 바와 플로팅 버튼(FAB)이 아래로 미끄러지듯 이동하여 안보이게 처리 (`translate-y-full`, `opacity-0 pointer-events-none`).
  - **스크롤 업 (Scroll Up)**: 스크롤을 올릴 때 부드러운 트랜지션 애니메이션(`duration-300`)과 함께 네비게이션 바와 FAB가 다시 화면에 나타남 (`translate-y-0`, `opacity-100`).

### 🗓️ [탭 1] 스케줄러 (Calendar Scheduler)
- **2026년 9월 직관적 달력 UI**:
  - 일~토 7일 그리드 방식. **9월 4일~6일** 행사 일정 강조 및 클릭 시 '🚨 현장 레이드' 탭으로 원클릭 화면 전환.

### 🚨 [탭 2] 현장 레이드 (Real-Time Live Raid)
- **실시간 dynamic 경과 시간 (Real-Time Timestamp)**:
  - 게시물 생성 시각(`createdAt`) 기반으로 `방금 전`, `🔥 2분 전`, `15분 전`, `1시간 전` 등 실시간(매 10초 타이머) 자동 계산 반영.
- **피드백 & 상태 영구 동적 저장 (LocalStorage Persistence)**:
  - `👍 도움돼요`, `👎 정보가 달라요` 클릭 시 수치가 `localStorage`에 즉시 반영되며 새로고침 후에도 유지.
- **내가 작성한 글 식별 & 👑 뱁새 관리자 뱃지**:
  - 작성 제보 글에 **`👤 내가 작성함`** 핑크 뱃지, **`👑 뱁새 관리자`** 황금 뱃지, 핑크 강조 테두리, `🗑️ 삭제` 버튼 제공.
- **저신뢰도 카드 50% 투명화 & 뱃지**:
  - `dislikes >= likes` 시 카드 전체 및 텍스트/이모지 투명도가 **50% (`opacity-50`)로 감소**하며 **`⚠️ 교차검증 필요`** 뱃지 자동 부착.
- **제보하기 태그 박스(Chip) UX & `+` 커스텀 추가**:
  - 클릭 가능한 태그 박스 + 맨 우측 **`+` 태그 박스**로 신규 제보 태그 생성. 가장 최근 사용 태그 **맨 왼쪽(1st)**, 나머지 **사용량 순서** 정렬.

### 🔄 [탭 3] 굿즈 스왑 (Goods Swap Exchange)
- **실시간 굿즈 교환 피드 & 내 스왑 글 표시**:
  - 작성 스왑 글에 **`👤 내가 작성함`** 뱃지와 강조 테두리 부여. 실시간 경과 시간 계산 및 `localStorage` 영구 반영.
- **수익 모델 (100원 확성기) 상단 고정 정렬**:
  - `isBoosted: true` ('📢 100원 확성기로 상단 노출 중') 속성 카드가 피드 **무조건 최상단(1~2위)**에 정렬.
- **카테고리 태그 박스(Chip) UX & `+` 커스텀 추가**:
  - 카테고리 태그 박스 + **`+` 커스텀 태그** 생성. 최근 사용 카테고리 **맨 왼쪽(1st)**, 나머지 **사용 빈도순** 정렬.

### ℹ️ aT센터 인프라 & 부스 배치도 모달
- 부스 배치도(A~E구역), 코인 락커(지하 1층), ATM 위치(1층 로비) 안내.

---

## 📜 3. 버전별 히스토리 & 변경 이력 (Version History)

### 🔹 v1.0 ~ v1.4
- 초기 개발자 버전에서 서브컬처 페스티벌 컨셉 개편, 3개 탭 확장, 뱁새 테마 토글, 50% 저신뢰도 투명화 & 태그 박스 UX 구현.

### 🔹 v1.5 (반응형 텍스트 튀어나옴 수정, 실시간 dynamic 시간 & 내 글/관리자 뱃지)
- 카드 텍스트 튀어나옴 완벽 수정, relative timestamp 매 10초 갱신, `localStorage` 수치 동적 저장, `👤 내가 작성함` 및 `👑 뱁새 관리자` 뱃지 적용.

### 🔹 v1.6 (핸드폰 다이나믹 아일랜드 / 노치 / 상태바 Safe Area Inset 완벽 방지)
- `env(safe-area-inset-top)` & `env(safe-area-inset-bottom)` 환경 변수를 상단 헤더(`header-safe-top`)와 하단 네비게이션 바(`nav-safe-bottom`), FAB(`fab-safe-bottom`)에 완벽 적용하여 아이폰 다이나믹 아일랜드, 노치, 상단 상태바(시계, 배터리 등) 및 하단 홈 바와 UI가 겹치는 현상을 전면 해결.

### 🔹 v1.7 (스크롤 방향 감지 하단 탭 & FAB 숨김/보임 애니메이션)
- 스크롤 내려갈 때(Scroll Down) 하단 탭(`🗓️ 스케줄러`, `🚨 현장 레이드`, `🔄 굿즈 스왑`)과 FAB 버튼이 아래로 숨겨져(`translate-y-full`) 콘텐츠 시야를 확보하고, 스크롤 올릴 때(Scroll Up) 부드럽게 다시 나타나도록 dynamic scroll transition (`duration-300`) 적용. PWA Service Worker v4 갱신.

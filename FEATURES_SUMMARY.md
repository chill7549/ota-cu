# Ota-Cu (v1.0 MVP) 전체 기능 명세서 & 버전별 변경 이력

## 📌 1. 개요 및 실행 정보
- **앱 명칭**: Ota-Cu (서브컬처 일러스트 페스티벌 현장 레이드 & 굿즈 스왑 플랫폼)
- **프로젝트 위치**: `C:\Users\nikor\.gemini\antigravity\scratch\ota-cu`
- **로컬 실행 주소**: `http://localhost:3000`

---

## 🌟 2. 최신 현재 기능 종합 명세 (Current Full Features)

### 🎨 테마 & UI/UX 디자인 시스템
- **🕊️ 뱁새 테마 (Warm Light) ↔ 🌙 다크 테마 (Dark Mode) 동적 전환**
  - 헤더 우측 상단 토글 버튼으로 실시간 전환.
  - **뱁새 테마**: 포근한 웜 화이트 (`#fbf9f5`) 배경, 차콜 그레이/소프트 브라운 텍스트, 파스텔 핑크 포인트 컬러셋 적용.
  - **다크 테마**: 가독성 높은 다크 슬레이트 톤과 인디고/바이올렛 그래디언트 포인트.
- **📱 고정 하단 네비게이션 바 (Bottom Navigation Bar)**
  - 3개 핵심 탭 (`🗓️ 스케줄러`, `🚨 현장 레이드`, `🔄 굿즈 스왑`)을 화면 최하단에 고정.
- **📍 플로팅 액션 버튼 (FAB)**
  - 하단 네비게이션 바로 위 (`bottom-20 right-4`)에 둥근 플로팅 형태로 위치 (`+` 제보하기 / `🔄 굿즈 교환 글 올리기`).
- **📐 하단 스크롤 안전 여백 (`pb-40`)**
  - 스크롤 메인 영역 하단에 `pb-40` 패딩을 보장하여 탭 바와 FAB가 마지막 게시글을 가리지 않음.

### 🗓️ [탭 1] 스케줄러 (Calendar Scheduler)
- **2026년 9월 직관적 달력 UI**:
  - 일~토 7일 그리드 방식.
  - **9월 4일~6일** 행사 일정(일러스트코리아 in aT)을 감각적인 날짜 블록 및 메인 카드로 강조 렌더링.
- **원클릭 탭 자동 전환**:
  - 9/4~6일 날짜 블록이나 `🎨 일러스트코리아 in aT` 메인 카드를 클릭하면 즉시 **'🚨 현장 레이드'** 탭으로 자동 화면 전환.

### 🚨 [탭 2] 현장 레이드 (Real-Time Live Raid)
- **현장 혼잡도 & 지표 카드**:
  - aT센터 혼잡도(입장 대기시간), 선명한 노란색 펄스(`animate-pulse`) 인디케이터, 티켓 발권 상태, 3대 실시간 카운터 지표 (`[실시간 제보 N건] [교환 중 N건] [전시장 현황: 쾌적]`).
- **정보 신뢰도 (변별력) 시스템**:
  - **10분 이내 신규 제보**: 빨간색 강조 텍스트 (`🔥 방금 전`, `🔥 5분 전`).
  - **30분 이상 경과 제보**: 차분한 회색 텍스트 (`11:00 (35분 전)`).
  - **교차 검증 버튼**: `👍 도움돼요` 옆에 `👎 정보가 달라요` 버튼을 나란히 배치.
  - **저신뢰도 카드 50% 투명화 & 뱃지**: `dislikes >= likes` (피드백 동률 또는 초과) 시 카드 전체 및 텍스트/이모지 투명도가 **50% (`opacity-50`)로 감소**하며 **`⚠️ 교차검증 필요`** 뱃지 자동 부착.
  - **제보자 신뢰도 뱃지**: 신뢰도 우수 제보자 닉네임 옆에 **`👑 베테랑 뱁새`** 뱃지 부여.
- **실시간 제보 태그 박스(Chip) UX & `+` 커스텀 추가**:
  - 드롭다운 대신 클릭 가능한 태그 박스 (기본: `대기열`, `품절 소식`, `부스 공지`).
  - 맨 우측 **`+` 태그 박스**로 사용자가 원하는 제보 태그 직접 생성.
  - **스마트 정렬**: 가장 최근에 사용한 태그가 **맨 왼쪽(1st)**에 고정되고, 오른쪽으로 갈수록 **사용량(Usage Count)이 많은 순서**대로 나열.

### 🔄 [탭 3] 굿즈 스왑 (Goods Swap Exchange)
- **실시간 굿즈 교환 피드**:
  - `A-04 부스 홀로그램 엽서`, `B-02 작가님 아크릴 스탠드` 등 서브컬처 굿즈 항목.
  - `양도(Have)` vs `구함(Want)`, 접선 장소, `교환가능/완료` 상태 토글.
- **수익 모델 (100원 확성기) 상단 고정 정렬**:
  - `isBoosted: true` ('📢 100원 확성기로 상단 노출 중') 속성 카드가 피드 **무조건 최상단(1~2위)**에 정렬.
- **카테고리 태그 박스(Chip) UX & `+` 커스텀 추가**:
  - 클릭 가능한 카테고리 태그 박스 (기본: `스티커/지류`, `키링/아크릴`, `포스터/회지`).
  - 맨 우측 **`+` 태그 박스**로 신규 카테고리 직접 등록.
  - **스마트 정렬**: 가장 최근에 사용한 카테고리가 **맨 왼쪽(1st)**에 고정되고, 나머지는 **사용 빈도순**으로 자동 나열.
- **공유 카드 & 위치 핑**:
  - 트위터 공유용 블라인드 카드 생성기 및 접선 위치 텍스트 복사 모달.

### ℹ️ aT센터 인프라 & 부스 배치도 모달
- **헤더 우측 [ℹ️ 인프라] 버튼**:
  - **🗺️ 전시장 부스 배치도**: A구역(일러스트/키링), B구역(스티커/지류), C구역(포스터/팬아트), D구역(회지/북), E구역(기업 팝업).
  - **🔐 코인 락커**: 지하 1층 신분당선 연결 통로 위치 및 요금 안내.
  - **🏧 ATM 위치**: 1층 로비 NH농협/KB국민은행 ATM기 위치 안내.

---

## 📜 3. 버전별 히스토리 & 변경 이력 (Version History)

### 🔹 v1.0 MVP (초기 깃허브 개발자 연동 버전을 기반으로 시작)
- **최초 구현**: 개발자 컨퍼런스/깃허브 연동 기반의 2개 탭 (실시간 레이드 & 굿즈 스왑) 구현.
- **깃허브 REST API 연동**: PAT 연동 및 공개 리포지토리 이슈 타임라인 로드 기능.

### 🔹 v1.1 (서브컬처 일러스트 페스티벌 컨셉 전면 전환)
- **컨셉 개편**: 개발자 용어(Git, PR, Issue, CI)를 전면 제거하고 '일러스트코리아 in aT' 서브컬처 굿즈 페스티벌 컨셉으로 전환.
- **명칭 변경**: 앱 타이틀 `Ota-Cu (v1.0 MVP)` 설정.
- **3대 지표 추가**: `[실시간 제보 42건] [교환 중 18건] [전시장 현황: 쾌적]` 지표 신설.
- **혼잡도 원형 인디케이터**: 눈에 띄는 노란색 펄스(`animate-pulse`) 원형 인디케이터 적용.
- **기본 하단 패딩**: `pb-32` 패딩으로 CTA 바 및 버튼 가림 1차 수정.

### 🔹 v1.2 (3개 탭 확장, 캘린더 UI, 인프라 모달 & 확성기 정렬)
- **스케줄러 탭 신설**: 9월 달력 UI 구현 및 9/4~6일 일러스트코리아 행사 클릭 시 '현장 레이드' 탭으로 즉시 전환 기능 연결.
- **인프라 모달 추가**: `ℹ️ aT센터 인프라` 버튼으로 부스 배치도(A~E구역), 지하 1층 코인 락커, 1층 로비 ATM 팝업 구축.
- **수익 모델 강제 정렬**: `isBoosted = true` (100원 확성기) 게시글이 무조건 최상단 1~2위에 노출되도록 `Array.sort` 구현.
- **더미 데이터 정제**: 서브컬처 굿즈 명칭으로 데이터 클리닝.
- **하단 패딩 강화**: `pb-40`으로 여유 공간 확장.

### 🔹 v1.3 (뱁새 테마 토글, Bottom Nav & 신뢰도 시스템)
- **뱁새 테마 토글**: `🌙 다크` ↔ `🕊️ 뱁새` 토글 구현. 뱁새 테마용 웜 화이트 (`#fbf9f5`) 배경, 차콜 그레이 텍스트, 파스텔 핑크 포인트 컬러셋 구축.
- **Bottom Navigation Bar**: 3개 탭을 최하단 고정 네비게이션 바(Bottom Nav)로 이동.
- **FAB 위치 조정**: 플로팅 액션 버튼을 하단 바 바로 위 (`bottom-20 right-4`)에 둥근 형태 배치.
- **정보 신뢰도 시스템 도입**:
  - 10분 이내 제보 빨간색(`🔥 방금 전`), 30분 이상 지난 제보 회색 분리.
  - `👍 도움돼요` 옆에 `👎 정보가 달라요` 버튼 나란히 배치.
  - 우수 제보자 `👑 베테랑 뱁새` 뱃지 표기.

### 🔹 v1.4 (저신뢰도 50% 투명화, ⚠️ 뱃지 & 동적 태그 박스 UX)
- **저신뢰도 카드 50% 투명화 & 뱃지**: `dislikes >= likes` 발생 시 카드 전체 및 텍스트/이모지 투명도가 **50% (`opacity-50`)로 감소**하고 **`⚠️ 교차검증 필요`** 뱃지 자동 부착.
- **현장 레이드 태그 박스 UX**: 드롭다운을 태그 박스(Chip)로 교체 및 `+` 버튼으로 커스텀 태그 추가 기능 구현. 최근 사용 태그 **맨 왼쪽(1st)** 고정 및 **사용량 순 정렬**.
- **굿즈 스왑 태그 박스 UX**: 카테고리를 태그 박스(Chip)로 교체 및 `+` 버튼으로 커스텀 카테고리 추가 기능 구현. 최근 사용 카테고리 **맨 왼쪽(1st)** 고정 및 **사용 빈도 순 정렬**.
- **프로젝트 디렉토리 변경**: 폴더명을 `git-raid-swap-app`에서 `ota-cu`로 정식 변경 및 로컬 서버 재구동.
